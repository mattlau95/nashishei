# Inbox

- Clicing on "View In your gallery" takes you to the image view (where "Browse /Edit Names, Slideshow, Copy Link buttons aer). clicking on the newly added thumbnail would do the same as "view in your gallery" in step 2 of 2 after "Saved!"
- in image view, "browse edit names" sticky works ok on Mobile but sometimes doesn't shopw when the image itself is portrait (taller than wider), will need to come up with a feasible solution.
