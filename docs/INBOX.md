# Inbox
- Floating button position — I picked top: var(--space-2), left: var(--space-2) (top-left corner of the image). The reasoning was: the × delete button is already at top-right of each box, so top-left of the image keeps affordances separated. Was that right, or did you have a preferred position in mind?
